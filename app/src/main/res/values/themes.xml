<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.KotlinCovidState" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light</item>
        <item name="colorPrimaryVariant">@color/light</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Background color. -->
        <item name="android:colorBackground">@color/light_bg</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/light</item>
        <item name="colorSecondaryVariant">@color/light</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/light</item>
    </style>

    <style name="Theme.LightDark" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:background">@color/light</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.TextLightDark" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:textColor">@color/black</item>
    </style>
</resources>